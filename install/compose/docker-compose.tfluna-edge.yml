services:
  tfluna_edge:
    container_name: mowgli-tfluna-edge
    image: ${RANGE_IMAGE:-ghcr.io/...}
    network_mode: host
    privileged: true
    volumes:
      - /dev:/dev
    restart: unless-stopped